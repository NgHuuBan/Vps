name: Run Minecraft Server 7GB (paper.jar + world.zip từ GitHub Releases)

on:
  workflow_dispatch:

jobs:
  mc-server:
    runs-on: ubuntu-latest

    steps:
    - name: Cài Java 21 (Temurin)
      run: |
        sudo apt update
        sudo apt install -y wget tar curl gpg unzip zip
        wget https://github.com/adoptium/temurin21-binaries/releases/download/jdk-21.0.3%2B9/OpenJDK21U-jre_x64_linux_hotspot_21.0.3_9.tar.gz
        mkdir -p ~/java21
        tar -xzf OpenJDK21U-jre_x64_linux_hotspot_21.0.3_9.tar.gz -C ~/java21 --strip-components=1
        echo "JAVA_HOME=$HOME/java21" >> $GITHUB_ENV
        echo "$HOME/java21/bin" >> $GITHUB_PATH

    - name: Tải bản backup mới nhất từ GitHub Releases (nếu có)
      run: |
        mkdir -p server
        curl -s https://api.github.com/repos/${{ github.repository }}/releases/latest \
        | grep browser_download_url \
        | grep server.zip \
        | cut -d '"' -f 4 \
        | xargs -r curl -L -o server.zip
        if [ -f server.zip ]; then unzip server.zip -d .; fi

    - name: Cài playit từ repo chính thức
      run: |
        curl -SsL https://playit-cloud.github.io/ppa/key.gpg | gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/playit.gpg >/dev/null
        echo "deb [signed-by=/etc/apt/trusted.gpg.d/playit.gpg] https://playit-cloud.github.io/ppa/data ./" | sudo tee /etc/apt/sources.list.d/playit-cloud.list
        sudo apt update
        sudo apt install -y playit

    - name: Mở port với playit.gg
      working-directory: server
      run: |
        playit > playit.log &
        sleep 10
        echo "🌐 Link Minecraft:"
        grep -oP 'tcp:\/\/.*playit\.gg:\d+' playit.log || echo "⏳ Đang chờ port..."

    - name: Tải paper.jar nếu chưa có
      working-directory: server
      run: |
        if [ ! -f paper.jar ]; then
          curl -L -o paper.jar https://api.papermc.io/v2/projects/paper/versions/1.21.4/builds/232/downloads/paper-1.21.4-232.jar
        fi

    - name: Tạo eula.txt tự động
      working-directory: server
      run: echo "eula=true" > eula.txt

    - name: Chạy Minecraft server với 7GB RAM
      working-directory: server
      run: |
        java -Xmx7G -Xms7G -jar paper.jar nogui || true
        echo "✅ Server đã tắt — giữ hoạt động thêm 6 tiếng"
        sleep 6h

    - name: Nén thư mục server thành server.zip
      run: |
        cd server
        zip -r ../server.zip .

    - name: Upload bản backup lên GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: backup-${{ github.run_id }}
        files: server.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
